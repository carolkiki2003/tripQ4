import Vue from 'vue'
import Router from 'vue-router'
Vue.use(Router)

const router = new Router({
  mode: 'history',
  // base: `${BASE_URL}/${FOLDER}`,
  routes: [
    // {
    //   path: '/',
    //   name: 'example',
    //   component: example
    // }
  ]
})
export default router
